#!/usr/bin/env ruby

require "json"

require 'bosh-init_deployment_resource'

working_dir = ARGV[0]
request = JSON.parse(STDIN.read)

command = BoshInitDeploymentResource::OutCommand.new()

begin
  command.run(working_dir, request)
rescue => e
  STDERR.puts e.message
  exit 1
end
